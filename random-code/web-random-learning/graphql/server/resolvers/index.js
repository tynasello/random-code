const Query = require('./Query');
const AnimalClass = require('./AnimalClass');
const Animal = require('./Animal');
const Mutation = require('./Mutation');

const resolvers = { Query, AnimalClass, Animal, Mutation };
module.exports = resolvers;
